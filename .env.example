# API Gateway base URL — all frontend requests go through this.
# Do not use internal microservice ports directly.
NEXT_PUBLIC_API_URL=http://localhost:8080

# Set to true to use local mock auth (no backend required).
# Set to false when Identity Service is running.
NEXT_PUBLIC_USE_MOCK_AUTH=false
